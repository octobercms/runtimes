[www]
listen = 127.0.0.1:9000
; Keep container/supervisor env (HOME, PGSSLCERT, DB_*, …) visible to pool workers.
; Required so libpq does not fall back to probing /root/.postgresql under www-data.
clear_env = no
